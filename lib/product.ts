export function publicUrl(raw:string){const u=new URL(/^https?:\/\//i.test(raw)?raw:'https://'+raw);const h=u.hostname.toLowerCase();if(!['https:','http:'].includes(u.protocol)||u.username||u.password||(u.port&&u.port!=='443'&&u.port!=='80')||!h.includes('.')||h.includes(':')||/^[\d.]+$/.test(h)||/(^|\.)(localhost|local|internal|lan|home|test|invalid)$/.test(h))throw Error('请输入有效的公开产品官网链接');u.hash='';return u;}
